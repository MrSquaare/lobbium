name: Pull Request

on:
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  server-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup environment
        uses: ./.github/actions/setup-env

      - name: Build
        working-directory: ./apps/server
        run: pnpm build

      - name: Lint
        working-directory: ./apps/server
        run: pnpm lint

  mobile-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup environment
        uses: ./.github/actions/setup-env

      - name: Export
        working-directory: ./apps/mobile
        run: pnpm export

      - name: Lint
        working-directory: ./apps/mobile
        run: pnpm lint

  mobile-android-build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup environment
        uses: ./.github/actions/setup-env

      - name: Get fingerprint
        id: fingerprint
        run: |
          echo "hash=$(pnpm dlx @expo/fingerprint fingerprint:generate --platform android | jq -r '.hash')" >> $GITHUB_OUTPUT

      - name: Generate prebuild
        working-directory: ./apps/mobile
        run: pnpm expo prebuild --platform android --clean --no-install

      - name: Cache Gradle
        uses: actions/cache@v4
        id: gradle
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ steps.fingerprint.outputs.hash }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Cache Build
        uses: actions/cache@v4
        id: build
        with:
          path: |
            apps/mobile/android/build
            apps/mobile/android/app/build
          key: ${{ runner.os }}-build-${{ steps.fingerprint.outputs.hash }}
          restore-keys: |
            ${{ runner.os }}-build-

      - name: Build
        working-directory: ./apps/mobile/android
        if: steps.gradle.outputs.cache-hit != 'true' || steps.build.outputs.cache-hit != 'true'
        run: ./gradlew assembleDebug --build-cache --parallel

  mobile-ios-build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup environment
        uses: ./.github/actions/setup-env

      - name: Set Xcode version
        run: sudo xcode-select --switch "/Applications/Xcode_16.4.app/Contents/Developer"

      - name: Get fingerprint
        id: fingerprint
        run: |
          echo "hash=$(pnpm dlx @expo/fingerprint fingerprint:generate --platform ios | jq -r '.hash')" >> $GITHUB_OUTPUT

      - name: Generate prebuild
        working-directory: ./apps/mobile
        run: pnpm expo prebuild --platform ios --clean --no-install

      - name: CocoaPods Cache
        uses: actions/cache@v4
        id: pods
        with:
          path: apps/mobile/ios/Pods
          key: ${{ runner.os }}-pods-${{ steps.fingerprint.outputs.hash }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Install CocoaPods
        working-directory: ./apps/mobile/ios
        run: pod install

      - name: Cache Xcode derived data
        uses: actions/cache@v4
        id: xcode-derived-data
        with:
          path: apps/mobile/ios/build
          key: ${{ runner.os }}-xcode-derived-data-${{ steps.fingerprint.outputs.hash }}
          restore-keys: |
            ${{ runner.os }}-xcode-derived-data-

      - name: Build
        working-directory: ./apps/mobile/ios
        if: steps.pods.outputs.cache-hit != 'true' || steps.xcode-derived-data.outputs.cache-hit != 'true'
        run: set -o pipefail && xcodebuild build -workspace Lobbium.xcworkspace -scheme Lobbium -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath build | xcbeautify
